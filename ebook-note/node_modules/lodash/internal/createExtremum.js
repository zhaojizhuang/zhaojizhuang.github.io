var arrayExtremum = require('lodash/internal/arrayExtremum'),
    baseCallback = require('lodash/internal/baseCallback'),
    baseExtremum = require('lodash/internal/baseExtremum'),
    isArray = require('lodash/lang/isArray'),
    isIterateeCall = require('lodash/internal/isIterateeCall'),
    toIterable = require('lodash/internal/toIterable');

/**
 * Creates a `_.max` or `_.min` function.
 *
 * @private
 * @param {Function} comparator The function used to compare values.
 * @param {*} exValue The initial extremum value.
 * @returns {Function} Returns the new extremum function.
 */
function createExtremum(comparator, exValue) {
  return function(collection, iteratee, thisArg) {
    if (thisArg && isIterateeCall(collection, iteratee, thisArg)) {
      iteratee = undefined;
    }
    iteratee = baseCallback(iteratee, thisArg, 3);
    if (iteratee.length == 1) {
      collection = isArray(collection) ? collection : toIterable(collection);
      var result = arrayExtremum(collection, iteratee, comparator, exValue);
      if (!(collection.length && result === exValue)) {
        return result;
      }
    }
    return baseExtremum(collection, iteratee, comparator, exValue);
  };
}

module.exports = createExtremum;
